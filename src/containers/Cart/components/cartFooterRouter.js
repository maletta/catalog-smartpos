import paths from 'paths';
import history from 'utils/history';

export const redirectToRegisterData = () => history.push(paths.registerData);
export const redirectToHome = () => history.push(paths.home);
