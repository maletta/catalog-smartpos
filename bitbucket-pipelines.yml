image: netpos/node-aws

pipelines:
  default:
    - step:
        name: 🔨 Build
        script:
          - yarn install
          - yarn build
    - step:
        name: ✨ Lint
        script:
          - yarn install
          - yarn lint
    - step:
        name: ✔️ Test
        script:
          - yarn install
          - yarn test
